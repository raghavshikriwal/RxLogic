# Leave unset in normal use — the app calls /api/* on the same origin
# it's served from, which is correct both in `npm run dev` (proxied to
# Flask, see vite.config.js) and in production (Flask serves the build).
# Only set this if the API is genuinely on a different origin.
VITE_API_BASE=